[project]
name = "notecomputer"
version = "0.0.1"
description = "notecomputer"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "牛哥", email = "farfarfun@qq.com" }]
dependencies = [
    "requests",
    "tqdm",
    "cryptography",
    "pycurl",
    "urllib3",
    "requests_toolbelt",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["notecomputer"]
